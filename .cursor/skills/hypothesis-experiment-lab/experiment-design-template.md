# 实验设计：[实验名称]

## 元信息

| 字段 | 内容 |
|------|------|
| 对应假设编号/陈述 | （如 H1：…） |
| 草稿名称 slug | `draft_slug`（与目录名 `lab_{draft_slug}_{exp_slug}` 一致） |
| 实验名称 slug | `exp_slug` |
| 目录 | `experiment/lab_{draft_slug}_{exp_slug}/` |

## 1. 目的

用 2–4 句话说明本实验要检验什么、与假设的对应关系。

## 2. 变量与对照

- **自变量**：…
- **因变量 / 指标**：…
- **控制变量**：…
- **基线或对照**：…

## 3. 数据与可重复性

- **数据来源**（合成 / 公开集 / 路径占位）：…
- **随机性**：随机种子字段在 `params.json5`（如 `rng_seed`）。
- **环境（每个 lab 独立；Agent 须按此执行）**：
  - **语言/运行时**：如 Python 3.10+。
  - **依赖清单路径**：本目录 `requirements.txt`（或写明若共用上级清单的相对路径及理由）。
  - **可复制命令**（示例，按实际入口文件名改）：`python -m venv .venv && source .venv/bin/activate`（Windows 用 `.venv\Scripts\activate`）→ `pip install -r requirements.txt` → `python run.py`。
  - **Agent 义务**：在本 `lab_*` 目录完成上述安装后再运行；失败则修正清单或版本并更新本节。

## 4. 步骤概要

1. …
2. …
3. …

（实现细节以同目录代码为准。）

## 5. 判据与解释

- **若假设成立**，预期观测到：…
- **若假设不成立**，可能表现为：…
- **提前停止条件**（可选）：…

## 6. 风险与局限

- …

## 7. 产物

- `params.json5`：全部可调参数（含用于对照实验的多组配置时，用数组或嵌套对象区分）。
- 代码入口文件名：…（如 `run.py`）
- 输出路径约定：…（建议也在 `params.json5` 的 `output` 段）
